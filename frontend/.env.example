# Copy this file to .env.local and fill in your values.
#
# NEXT_PUBLIC_API_KEY must match API_KEY in backend/.env exactly.
# Both are set once — generate with:  openssl rand -hex 32

NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
NEXT_PUBLIC_API_KEY=change-me-generate-with-openssl-rand-hex-32
